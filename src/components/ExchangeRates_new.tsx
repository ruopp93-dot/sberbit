"use client";

export function ExchangeRates() {
  // lightweight stub kept for compatibility; real component is in ExchangeRates.tsx
  return (
    <div style={{ color: 'var(--foreground)' }} className="p-4">
      <p style={{ color: 'var(--foreground)', opacity: 0.85 }}>Курсы отображаются в разделе Курсы.</p>
    </div>
  );
}
